# --- Python ---
.venv/
__pycache__/
*.pyc

# --- Large model weights (download separately — see README "Setup") ---
# Trained classifier weights, ~421 MB, exceeds GitHub's 100 MB file limit.
models/bert_bigru_stage1.pt

# --- Regenerated locally by scripts/build_retrieval_index.py ---
models/retrieval.index
models/retrieval_embeddings.npy

# --- Frozen stage-1 originals (unused) + rolling backups ---
models/hazard.index
models/flagged_embeddings.npy
models/backups/

# NOTE: models/flagged_reviews.parquet IS committed — it's the source dataset
# and the retrieval store above is rebuilt from it.

# --- Runtime / local tooling ---
server.log
.claude/
